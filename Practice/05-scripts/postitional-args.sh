#!/bin/bash

# Positional arguments are zero-indexed, starting with the script itself:
#
# ./myscript.sh              <-- this would be the 0 argument
# ./myscript.sh subcommand   <-- "subcommand" would be the 1 argument
# ... and so on ...
#

